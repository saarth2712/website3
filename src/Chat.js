import { useState, useRef, useEffect } from "react";

const Chat = (props) => {
  const userId = useRef("ram");
  //   const recipientId = useRef("hello");
  return (
    <div style={{ fontSize: "50px" }}>
      Your chat between and starts here. Hi
    </div>
  );
};

export default Chat;
